Run the local CI checks to verify the project builds and lints cleanly.

Steps:
1. Run `pnpm lint` and fix any linting issues
2. Run `pnpm build` and fix any build errors
3. Report a summary of results
